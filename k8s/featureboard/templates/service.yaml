apiVersion: v1
kind: Service
metadata:
  name: featureboard
  namespace: featureboard
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: featureboard
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000